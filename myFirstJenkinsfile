#!groovy
pipeline {
    agent any
    stages {
        stage('build') {
           steps {
              echo 'My First Jenkinspipe build'
            }
        }
        stage('test') {
           steps {
              echo 'My First Jenkinspipe test'
            }
        }
        stage('deploy') {
           steps {
              echo 'My First Jenkinspipe deploy'
            }
        }
    }
}
