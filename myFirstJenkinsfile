pipeline {
    agent any
    stages {
        stage("build") {
           steps {
              echo "My First Jenkinspipe build"
            }
        }
    }
    stages {
        stage("build") {
           steps {
              echo "My First Jenkinspipe build"
            }
        }
    }
    stages {
        stage("build") {
           steps {
              echo "My First Jenkinspipe build"
            }
        }
    }
}
